
location / {
    proxy_pass http://app;
    proxy_set_header X-Authenticated $oidc_authenticated;
    proxy_set_header X-Id-Token $oidc_id_token;
    proxy_set_header X-Access-Token $oidc_access_token;
    proxy_set_header X-UserInfo $oidc_userinfo;
    proxy_set_header X-User-ID $oidc_claim_sub;
}

location = /bearer {
    auth_oidc_bearer on;
    proxy_pass http://app;
    proxy_set_header X-Authenticated $oidc_authenticated;
    proxy_set_header X-Id-Token $oidc_id_token;
    proxy_set_header X-Access-Token $oidc_access_token;
    proxy_set_header X-UserInfo $oidc_userinfo;
    proxy_set_header X-User-ID $oidc_claim_sub;
}

location = /bearer-verify {
    auth_oidc_bearer on;
    auth_oidc_mode verify;
    proxy_pass http://app;
    proxy_set_header X-Authenticated $oidc_authenticated;
    proxy_set_header X-Id-Token $oidc_id_token;
    proxy_set_header X-Access-Token $oidc_access_token;
    proxy_set_header X-UserInfo $oidc_userinfo;
    proxy_set_header X-User-ID $oidc_claim_sub;
}

location = /bearer-aud-other {
    auth_oidc_bearer on;
    auth_oidc_bearer_audience "other-api";
    proxy_pass http://app;
    proxy_set_header X-Authenticated $oidc_authenticated;
    proxy_set_header X-User-ID $oidc_claim_sub;
}

location = /bearer-aud-test {
    auth_oidc_bearer on;
    auth_oidc_bearer_audience "test";
    proxy_pass http://app;
    proxy_set_header X-Authenticated $oidc_authenticated;
    proxy_set_header X-User-ID $oidc_claim_sub;
}

location = /bearer-typ {
    auth_oidc_bearer on;
    auth_oidc_bearer_typ "at+jwt";
    proxy_pass http://app;
    proxy_set_header X-Authenticated $oidc_authenticated;
    proxy_set_header X-Id-Token $oidc_id_token;
    proxy_set_header X-Access-Token $oidc_access_token;
    proxy_set_header X-UserInfo $oidc_userinfo;
    proxy_set_header X-User-ID $oidc_claim_sub;
}

location = /no-bearer {
    proxy_pass http://app;
    proxy_set_header X-Authenticated $oidc_authenticated;
}

location = /no-bearer-verify {
    auth_oidc_mode verify;
    proxy_pass http://app;
    proxy_set_header X-Authenticated $oidc_authenticated;
}

location = /bearer-helper {
    auth_oidc off;
    content_by_lua_block {
        local http = require "resty.http"
        local oidc_test = require "oidc_test"
        local dict = ngx.shared.cookie_dict

        local args = ngx.req.get_uri_args()
        local path = args.path or "/bearer"
        local auth = args.auth or "none"
        local use_cookie = args.cookie == "valid"

        local httpc = http.new()

        local function ensure_valid()
            local id_token = dict:get("bearer_id_token")
            local access_token = dict:get("bearer_access_token")
            local cookie = dict:get("bearer_cookie")
            if id_token and access_token and cookie then
                return id_token, access_token, cookie
            end

            local res = oidc_test.full_flow(httpc)
            if not res then
                ngx.log(ngx.ERR, "bearer-helper: full_flow failed")
                return nil, nil, nil
            end

            id_token = string.match(res.body, "id%-token:([^\n]+)")
            access_token = string.match(res.body, "access%-token:([^\n]+)")
            local set_cookie = res.headers["Set-Cookie"]

            dict:set("bearer_id_token", id_token)
            dict:set("bearer_access_token", access_token)
            dict:set("bearer_cookie", set_cookie)

            return id_token, access_token, set_cookie
        end

        local headers = {}

        if auth == "valid" then
            local _, access_token = ensure_valid()
            headers["Authorization"] = "Bearer " .. access_token
        elseif auth == "id-token" then
            local id_token = ensure_valid()
            headers["Authorization"] = "Bearer " .. id_token
        elseif auth == "invalid" then
            headers["Authorization"] = "Bearer not-a-jwt"
        elseif auth == "bad-signature" then
            headers["Authorization"] =
                "Bearer eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9" ..
                ".eyJzdWIiOiJ0ZXN0In0.invalidsignaturevalue"
        elseif auth == "empty" then
            headers["Authorization"] = "Bearer" .. string.rep(" ", 1)
        elseif auth == "empty-space" then
            headers["Authorization"] = "Bearer" .. " \t "
        elseif auth == "no-separator" then
            headers["Authorization"] = "Bearer"
        elseif auth == "malformed" then
            headers["Authorization"] = "BearerAbc xyz"
        elseif auth == "basic" then
            headers["Authorization"] = "Basic dGVzdDp0ZXN0"
        end
        -- auth == "none": no Authorization header sent

        if use_cookie then
            local _, _, cookie = ensure_valid()
            headers["Cookie"] = cookie
        end

        local res, err = httpc:request_uri("http://127.0.0.1:1984" .. path, {
            follow_redirects = false,
            headers = headers,
        })
        if not res then
            ngx.log(ngx.ERR, "bearer-helper: request failed: ", err)
            return
        end

        ngx.status = res.status

        local forward_headers = { "WWW-Authenticate", "Location" }
        for _, h in ipairs(forward_headers) do
            local val = res.headers[h]
            if val then
                ngx.header[h] = val
            end
        end

        ngx.print(res.body)
    }
}

location = /bearer-typ-helper {
    auth_oidc off;
    content_by_lua_block {
        local http = require "resty.http"
        local oidc_test = require "oidc_test"

        local args = ngx.req.get_uri_args()
        local httpc = http.new()

        if args.state then
            oidc_test.set_state(httpc, "access_token", args.state)
        end

        local res = oidc_test.full_flow(httpc)

        if args.state then
            oidc_test.clear_state(httpc)
        end

        if not res then
            ngx.log(ngx.ERR, "bearer-typ-helper: full_flow failed")
            ngx.status = 500
            return
        end

        local access_token = string.match(res.body, "access%-token:([^\n]+)")

        local bres, err = httpc:request_uri("http://127.0.0.1:1984/bearer-typ", {
            follow_redirects = false,
            headers = { ["Authorization"] = "Bearer " .. access_token },
        })
        if not bres then
            ngx.log(ngx.ERR, "bearer-typ-helper: request failed: ", err)
            return
        end

        ngx.status = bres.status

        local auth_header = bres.headers["WWW-Authenticate"]
        if auth_header then
            ngx.header["WWW-Authenticate"] = auth_header
        end
    }
}
